spring.cloud.consul.discovery.preferIpAddress: true
spring.cloud.consul.discovery.enabled: true

spring.cloud.consul.host: {spring.cloud.consul.host}
---
spring:
  application:
    name: restclientboot
    profiles: consul
#spring.cloud.consul.host: consul-8500